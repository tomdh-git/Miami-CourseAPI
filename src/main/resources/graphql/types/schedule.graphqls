type Schedule {
    courses: [Course]
    freeTime: Int
}

union ScheduleResult = SuccessSchedule | ErrorSchedule

type SuccessSchedule {
    schedules: [Schedule]
}

type ErrorSchedule {
    error: String
    message: String
}